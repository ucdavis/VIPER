pipeline {
    agent any
    options {
        skipStagesAfterUnstable()
    }
    stages { 
        stage('Build') {
            steps { 
                bat "dotnet publish ./web/Viper.csproj -c \"Release\" /p:EnvironmentName=Test"
            }
        }
    }
}